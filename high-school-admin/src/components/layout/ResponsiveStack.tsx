import type { ReactNode } from 'react'
export interface ResponsiveStackProps { children: ReactNode; direction?: { base?: 'row' | 'col'; sm?: 'row' | 'col'; md?: 'row' | 'col'; lg?: 'row' | 'col' }; align?: 'start' | 'center' | 'end' | 'stretch' | 'baseline'; justify?: 'start' | 'center' | 'end' | 'between' | 'around' | 'evenly'; gap?: 'none' | 'xs' | 'sm' | 'md' | 'lg' | 'xl'; wrap?: boolean; className?: string }
const gaps = { none: 'gap-0', xs: 'gap-2', sm: 'gap-3', md: 'gap-4', lg: 'gap-6', xl: 'gap-8' }
export function ResponsiveStack({ children, direction = { base: 'col', sm: 'row' }, align = 'stretch', justify = 'start', gap = 'md', wrap = false, className = '' }: ResponsiveStackProps) { return <div className={`flex flex-${direction.base ?? 'col'} ${direction.sm ? `sm:flex-${direction.sm}` : ''} ${direction.md ? `md:flex-${direction.md}` : ''} ${direction.lg ? `lg:flex-${direction.lg}` : ''} items-${align} justify-${justify} ${gaps[gap]} ${wrap ? 'flex-wrap' : ''} ${className}`}>{children}</div> }
