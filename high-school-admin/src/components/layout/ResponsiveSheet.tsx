import type { ReactNode } from 'react'
export interface ResponsiveSheetProps { isOpen: boolean; onClose: () => void; title?: ReactNode; description?: ReactNode; children: ReactNode; footer?: ReactNode; size?: 'sm' | 'md' | 'lg' | 'xl' | 'full'; showCloseButton?: boolean }
export function ResponsiveSheet({ isOpen, onClose, title, description, children, footer }: ResponsiveSheetProps) { if (!isOpen) return null; return <div className="fixed inset-0 z-50 flex items-end sm:items-center justify-center p-4"><button type="button" aria-label="Close" className="fixed inset-0 bg-black/60" onClick={onClose} /><section role="dialog" className="relative z-10 w-full max-w-lg rounded-2xl bg-white p-6 dark:bg-stone-900"><header className="mb-4"><h2 className="font-bold">{title}</h2>{description && <p className="text-sm">{description}</p>}</header>{children}{footer && <footer className="mt-4 flex justify-end gap-2">{footer}</footer>}</section></div> }
