import type { ReactNode } from 'react'
export interface ResponsiveScreenProps { children: ReactNode; title?: string; subtitle?: string; actions?: ReactNode; icon?: ReactNode; mobileActionBar?: ReactNode; maxWidth?: string; padding?: 'none' | 'compact' | 'normal' | 'spacious'; className?: string; headerClassName?: string; fullHeight?: boolean }
export function ResponsiveScreen({ children, title, subtitle, actions, icon, mobileActionBar, className = '', headerClassName = '' }: ResponsiveScreenProps) { return <div className={`w-full mx-auto ${className}`}>{(title || subtitle || actions || icon) && <header className={`mb-6 flex flex-col gap-3 sm:flex-row sm:items-center sm:justify-between ${headerClassName}`}><div className="flex items-start gap-3">{icon}<div>{title && <h1 className="text-2xl font-bold">{title}</h1>}{subtitle && <p className="text-sm">{subtitle}</p>}</div></div>{actions}</header>}<div>{children}</div>{mobileActionBar}</div> }
